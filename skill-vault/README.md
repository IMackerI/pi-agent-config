# Skill Vault

Not auto-loaded.

Store reusable opt-in skills here and scope them per project by adding their absolute paths to `.pi/settings.json`.

Recommended workflow:
- use `/scope-skill` for interactive project scoping
- or add skill directory paths manually to `.pi/settings.json`

Current contents currently include the moved frontend design skills, including themed `frontend-design-*` skills and the broad `frontend-design-general` skill.
